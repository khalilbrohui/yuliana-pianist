# Enable URL Rewriting
RewriteEngine On
RewriteBase /

# Route all requests to index.php if the requested file/folder does not physically exist
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [L,QSA]
